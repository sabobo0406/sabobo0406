# competitor_report / dashboard は標準ライブラリのみで動くため依存なし

# transcribe_draft: ローカル文字起こし(初回実行時にモデルを自動ダウンロード)
# ffmpeg が別途必要です: https://ffmpeg.org/download.html
faster-whisper>=1.0.0

# transcribe_draft の記事下書きは Claude Code CLI(サブスク)を使用:
#   npm install -g @anthropic-ai/claude-code
